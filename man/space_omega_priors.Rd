% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{space_omega_priors}
\alias{space_omega_priors}
\title{Generate evenly spaced priors for candidate breakpoints}
\usage{
space_omega_priors(K, tau_min, tau_max)
}
\arguments{
\item{K}{Number of candidate breakpoints.}

\item{tau_min}{Minimum value of the time/covariate variable.}

\item{tau_max}{Maximum value of the time/covariate variable.}
}
\value{
A list of length \code{K} containing prior specifications for \code{omega}.
}
\description{
This helper function generates a list of \code{prior_normal} objects for \code{omega}
(breakpoint locations) that are evenly spaced across the range of your time
variable \code{tau}. This is highly recommended when using \code{smoothbp_ss()} to
ensure the candidate breakpoints cover the entire domain without clumping.
}
\details{
For hierarchical models where \code{omega} has random effects (e.g., \code{~ 1 + (1 | group)}),
this function automatically names the prior \code{(Intercept)} so it applies correctly
to the global market mean, while the random effects are handled automatically by
the \code{sigma_re_om} shrinkage variance.
}
