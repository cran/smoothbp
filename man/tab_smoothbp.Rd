% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_model.R
\name{tab_smoothbp}
\alias{tab_smoothbp}
\title{Fixed-effects table for smoothbp_fit objects}
\usage{
tab_smoothbp(
  ...,
  labels = NULL,
  digits = 2,
  fmt = c("mean [CI]", "mean (SD)"),
  show_rhat = FALSE
)
}
\arguments{
\item{...}{One or more \code{smoothbp_fit} or \code{smoothbp_ss_fit} objects.}

\item{labels}{Character vector of column headers, one per model.  If \code{NULL}
(default) the deparsed call names are used.}

\item{digits}{Integer; number of decimal places (default \code{2}).}

\item{fmt}{Cell format: \code{"mean [CI]"} (default) shows mean and 95\% credible
interval; \code{"mean (SD)"} shows mean and posterior SD.}

\item{show_rhat}{Logical; append \eqn{\hat{R}} to each cell (default
\code{FALSE}).}
}
\value{
A \code{gt_tbl} object (or a \code{knitr_kable} if \strong{gt} is not installed).
}
\description{
Collects posterior summaries from one or more \code{smoothbp_fit} objects and
displays them in a single table with parameters on rows and models in
columns.  All parameters present in any model are shown; models that do not
include a given parameter display a dash.
}
\examples{
\dontrun{
tab_smoothbp(m.ep.pw1, m.wo.pw1, m.la.pw1,
             labels = c("Episodic", "Working", "Language"))
}
}
